# Secret for K8s/Openshift Deployment

kubectl create secret docker-registry image-secret --docker-server=docker.pkg.github.com --docker-username=USERNAME --docker-password=GITHUB_TOKEN --docker-email=EMAIL